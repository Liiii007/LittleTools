#include "Timer.h"
std::chrono::steady_clock::time_point Timer::_t1 = std::chrono::steady_clock::now();
std::chrono::steady_clock::time_point Timer::_t2 = std::chrono::steady_clock::now();
double Timer::duration = -1;